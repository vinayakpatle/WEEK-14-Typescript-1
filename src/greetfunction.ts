function greetFunction(user: string){   // instead of string we can write any(it'll allow user to assign any type value into user but then what's use use of string so you should use string)
    console.log(`Hello ${user}`);
}

greetFunction("Vinayak");